rules_version = '2';

// All client access goes through signed URLs minted by the Cloud Function
// `requestCardDbUpload`. Direct read/write from the SDK is denied.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
